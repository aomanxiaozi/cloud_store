package com.cloud.store.feign.fallback;

import com.cloud.store.feign.StoreFeignClient;
import org.springframework.stereotype.Component;

@Component
public class StoreFeignClientFallback implements StoreFeignClient {
}
